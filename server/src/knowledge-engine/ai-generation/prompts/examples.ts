export function renderIllustrativeExamples(): string {
  return `ANTI-PATTERN — never produce output like this for "Machine Learning": ["Cool AI Stuff", "Big Data", "Future of Tech", "Neural Network (Advanced)", "Neural Network (Basics)", "Some Algorithms"]. Wrong because: buzzwords instead of real subdivisions, an unrelated tangent, a near-duplicate pair that should be one "Neural Networks" node with its own children, and a meaningless catch-all. Nothing here would appear in a real textbook's table of contents.`;
}
